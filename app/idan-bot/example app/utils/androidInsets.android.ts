import { EdgeInsets } from 'react-native-safe-area-context';

export const getAndroidTopInset = (insets: EdgeInsets) => insets.top;
export const getAndroidBottomInset = (insets: EdgeInsets) => insets.bottom;
